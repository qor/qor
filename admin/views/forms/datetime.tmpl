<div class="form-group">
  <label for="{{.InputId}}">{{rt .Base .Label }}</label>
  <input type="text" class="form-control" id="{{.InputId}}" data-toggle="qor.datepicker" name="{{.InputName}}" value="{{.Value}}" {{if not (has_update_permission .Meta)}}DISABLED{{end}}>
</div>

