<div>
  <label class="label" for="{{.InputId}}">{{.Label}}</label>
  {{if .Value.URL}}
  <img src="{{.Value.URL}}">
  {{end}}
  <input id="{{.InputId}}" name="{{.InputName}}" value="{{.Value}}" type="file">
</div>
