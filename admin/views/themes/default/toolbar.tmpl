{{if .CurrentUser}}
<div class="qor-toolbar">
  <p class="navbar-text">{{t "Logged in as {{$1}}" .CurrentUser.DisplayName}}</p>

  <ul class="qor-nav-menu">
    <li>
      <a href="{{logout_url}}"><i class="md md-24">exit_to_app</i></a>
    </li>
  </ul>
</div>
{{end}}
