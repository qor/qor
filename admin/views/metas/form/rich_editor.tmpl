<div class="qor-field">
  <label class="qor-field__label" for="{{.InputId}}">{{dt (printf "%v.attributes.%v" .Base.ToParam .Label) .Label}}</label>
  <div class="qor-field__block">
    {{if .Meta.Resource}}
      <textarea class="qor-field__input" id="{{.InputId}}" data-toggle="qor.redactor" data-upload-url="{{url_for .Meta.Resource}}/upload" data-crop-url="{{url_for .Meta.Resource}}/crop" name="{{.InputName}}" rows="10" {{if not (has_update_permission .Meta)}}disabled{{end}}>{{if .Value}}{{.Value}}{{end}}</textarea>
    {{else}}
      <textarea class="qor-field__input" id="{{.InputId}}" data-toggle="qor.redactor" name="{{.InputName}}" rows="10" {{if not (has_update_permission .Meta)}}disabled{{end}}>{{if .Value}}{{.Value}}{{end}}</textarea>
    {{end}}
  </div>
</div>
