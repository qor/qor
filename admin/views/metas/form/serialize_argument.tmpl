{{if .Value}}
  {{if .Value.Resource}}
  <div class="promotion-rules-argument qor-field qor-fieldset-container">
    <div class="qor-field__block">
      <fieldset id="{{.InputId}}">
        {{render_form (edit_sections .Value.Resource) .Value.Value}}
      </fieldset>
    </div>
  </div>
  {{end}}
{{end}}
