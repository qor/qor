<div class="qor-form-section clearfix">
  {{if .Title}}
    <div class="qor-form-section-title">{{.Title}}</div>
  {{end}}
  {{if .Title}}<div class="qor-fieldset">{{end}}
  {{range .Rows}}
    <div class="qor-form-section-rows qor-section-columns-{{.Length}} clearfix">
      {{.ColumnsHTML}}
    </div>
  {{end}}
  {{if .Title}}</div>{{end}}
</div>
