{{define "header"}}
<header class="header" id="top">
  <div class="wrapper clearfix">
    <a class="brand fl" href="{{.Admin.GetRouter.Prefix}}">{{t "QOR"}}</a>

    <div class="navbar fr">
      <ul class="nav clearfix">
        {{render "menus" menus}}
      </ul>
    </div>
  </div>
</header>
{{end}}
