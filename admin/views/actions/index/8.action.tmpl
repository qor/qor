{{$context := .}}
{{$allowed_actions := (allowed_actions $context.Resource.Actions $context.Action)}}
{{$resource := .Resource}}

{{if gt (len $allowed_actions) 0 }}
  <div class="qor-action-bulk-edit" data-toggle="qor.action.bulk">
    <div class="qor-action-forms hidden">
      {{range $action := $allowed_actions}}
        <a class="mdl-button mdl-button--colored mdl-button--raised mdl-js-button mdl-js-ripple-effect qor-action-button"  data-url="{{url_for $resource}}/{{$action.ToParam}}" href="{{url_for $resource}}/{{$action.ToParam}}" {{if not $action.Resource}}data-dataType="json" data-confirm="{{t "qor_admin.form.are_you_sure"}}" data-method="PUT"{{end}}>
          {{t (printf "%v.actions.%v" $resource.ToParam $action.Label) $action.Label}}
        </a>
      {{end}}
    </div>

    <div class="qor-action-bulk-buttons">
      <button class="mdl-button mdl-button--colored mdl-js-button mdl-js-ripple-effect qor-action--bulk" type="button">{{t "Bulk Edit"}}</button>
      <button class="mdl-button mdl-button--accent mdl-js-button mdl-js-ripple-effect qor-action--exit-bulk hidden" type="button">{{t "Exit Bulk Edit"}}</button>
    </div>
  </div>
{{end}}
