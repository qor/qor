<div>
  {{link_to "Localize" (new_resource_path .Resource)}}
  {{range $locale := editable_locales .}}
    {{$locale}}
  {{end}}
</div>

<div>
  {{range $locale := viewable_locales .}}
    {{$locale}}
  {{end}}
</div>

<article class="article clearfix">
  {{render "index/filter"}}

  {{render "index/table"}}
</article>
